require 'spec_helper'

describe Metaspider do
  it 'has a version number' do
    expect(Metaspider::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
